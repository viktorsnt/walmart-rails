// Import Actions
import {  } from './ShoppingCartActions';

// Initial State
const initialState = {};

const ShoppingCartReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ShoppingCartReducer;
